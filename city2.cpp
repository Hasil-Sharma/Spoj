#include <cstdio>
int main()
{
	int n,array[100000],i,min,count,index=1,flag,start;
	while(scanf("%d",&n) != EOF)
	{
		for(i=0; i < n; i++)
			scanf("%d",&array[i]);

		for(i=0; i < n; i++)
		{
			if(array[i] == 0) 
			{
				start = i;
				continue;
			}
			found = 
			
		}
		printf("Case %d: %d\n",index,count);
		index++;
	}
	return 0;
}