#include <stdio.h>
#include <string.h>
int main()
{
	int t,index,flag;
	char* string[120005];
	char* found;
	char find[] = "100";
	scanf("%d",&t);
	while(t--)
	{
		fscanf(stdin,"%s",string);
		flag = 0;
		while(strlen(string) != 0)
		{
			found = strstr(string,find);
			if(found)
			{
				
			}
			else
			{
				flag = 1;
				break;
			}

		}
		if(flag) printf("no\n");
		else printf("yes\n");
	}
}